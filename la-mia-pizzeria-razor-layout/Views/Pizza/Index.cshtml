@model IEnumerable<Pizza>
@{
    ViewData["Title"] = "Menu";
}

<section class="menu-section">
    <h2>Il nostro Menu</h2>
    <a asp-action="Create" class="btn btn-primary mb-3">Crea Nuova Pizza</a>
    <div class="menu-items">
        @if (Model == null || !Model.Any())
        {
            <p>Non sono presenti pizze sul nostro menu</p>
        }
        else
        {
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Nome</th>
                        <th>Descrizione</th>
                        <th>Prezzo</th>
                        <th>Immagine</th>
                        <th>Azioni</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var pizza in Model)
                    {
                        <tr>
                            <td>@pizza.Name</td>
                            <td>@pizza.Overview</td>
                            <td>@pizza.Price €</td>
                            <td><img src="@pizza.Image" alt="@pizza.Name" style="max-width: 50px;" /></td>
                            <td>
                                <a asp-action="Details" asp-route-id="@pizza.Id" class="btn btn-info">Dettagli</a>
                                <a asp-action="Edit" asp-route-id="@pizza.Id" class="btn btn-warning">Modifica</a>
                                @*<button asp-controller="Pizza" asp-action="Delete" asp-route-id="@pizza.Id" class="btn btn-danger btn-sm cancel-button" data-id="@pizza.Id" data-name="@pizza.Name">
                                    Cancella
                                </button>*@
                                <form asp-controller="Pizza" asp-action="Delete" asp-route-id="@pizza.Id" method="post" >
                                    @Html.AntiForgeryToken()
                                    <button class="btn btn-danger" type="submit">
                                        Cancella
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        }
    </div>
</section>
@*<div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="deleteModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h1 class="modal-title fs-5" id="deleteModalLabel">Attenzione</h1>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>
                    Sei sicuro di voler cancellare "<span id="modal-item-title">
                    </span>"?
                </p>
            </div>
            <div class="modal-footer">
                <input type="hidden" id="pizzaId" name="id" value="" />
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Chiudi</button>
                <form id="deleteForm" method="post">
                    @Html.AntiForgeryToken()
                    <button id="confirm-action" type="submit" class="btn btn-danger btn-sm cancel-button">
                        Cancella definitivamente
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>*@

